# Core Reduction Phase 6 — Gate Delta 재심사 v0.2

> 심사관: Opus (delta, 코드 스팟 체크 12건). 대상: `9817c5e` (Synthesis v1.1 +
> Phase 4 §8).
> **판정: 조건부 PASS.**
> - **P0(8건)·P1(Queue) 즉시 착수 승인** — 조건과 독립.
> - **P2(V3 셸) 착수 전 문서 조건 C-1~C-4 반영** (재심사 불요, Phase 7 spec에서
>   자체 확인). → **v1.2에서 전부 반영 완료.**

## 14건 판정: 13 RESOLVED / 1 NOT RESOLVED

- B-1~B-4, M-5~M-11, m12~m13: **RESOLVED** (각 항목 코드 근거로 실증 —
  needs_attention 데이터 실재, nextVisitCheckItem 파급 4곳 재확인 "철회가 옳은
  판단", B-3 해소를 "게이트가 제기한 양립 불가 딜레마의 정면 해소"로 평가,
  격리 장치 9종 전부 코드 실재 확인, 클릭 표의 현행 열이 코드와 일치).
- **m14: NOT RESOLVED** — v1.1의 row 4 조건 `미설정 ∨ (미설정 ∧ current-visit)`은
  `A ∨ (A∧B) ≡ A` 항등식으로 무변경이었고, `current-visit`는 코드에 없는 개념.
  → **C-1**: v1.2에서 row 4 "현행 유지" + row 9 HIDE 철회(KEEP)로 지적 철회 처리.

## 신규 발견 (v1.2 반영 완료)

- **[MAJOR] N-1 → C-2**: 좌측 요약 200px 예산이 v1.1이 스스로 추가한 항목(계산불가
  병기·부위 명단·auth 액션)을 미반영 — 결정론적 절단 규칙 신설 (부위 최대 2+`외 N`,
  계산불가는 칩 접미, auth 액션은 저장 줄 대체, 200px=규칙 보장 상한).
- **[MAJOR] N-2 → C-3**: Phase 4 본문 3곳(목업 CLEAR 행·§6 제거 문구·"재확인 0")이
  v1.1과 충돌한 채 잔존 — 목업 직접 수정 + §8.3 supersede 목록.
- **[MINOR] N-3**: 신선도 "행 제거" 방향의 근거 부재 — **(b)안 채택**: 행 유지 +
  `⟳ 갱신 실패 · 마지막 확인 HH:MM` + 재시도 (가용성 손실 없이 침묵 소실 0 충족).
- **[MINOR] N-4 → C-4**: §2.10 "예외 없음"과 배지 행 자기모순 — "또는 동등 이상의
  상시 가시 표식(배지)"로 규칙 문구 수정 + 학습 케이스 행 추가.
- **[MINOR] N-5**: concept ⑥ 1줄이 N≥2·판독 불가를 미규정 — `·` 나열/`외 N`/판독
  불가 라벨 원문 보존 추가.
- **[MINOR] N-6**: 격리 표가 DoctorWorkspace 자체 `key={session_id}`(:3714) 누락 —
  key 제거 + render-time reset 단독 책임 명시.

## 지시 3축 점검 결과
- "+ 다른 유형 입력 추가"의 개념 수 영향: **없음** (라벨 순감 −5).
- 좌/우 재분배 vs "재확인 비용 0": **한정 표현으로 정합** (요약 0 / 전문 1클릭).
- 신선도 최엄격 통일의 부작용: **실재** → N-3 (b)안으로 해소.

## 구현 진입 승인문

> P0(8건) 및 P1(Queue) 구현 진입을 승인한다. P2 착수 전 C-1~C-4 문서 반영 완료
> (v1.2). Phase 7 spec은 §2.2 union 조건식·§2.8 통합 리셋 키·§2.10 조건식 표를
> **테스트 이름 단위의 검증 가능한 형태**로 옮겨 적는 것을 첫 산출물로 한다.

## Phase 7/8 감시 리스크 상위 3
1. **fail-open 재발** — P0-1이 profile 게이트를 실제로 끊었는지; herbal 파생
   환자에서 레인1 전문이 렌더되는지 테스트로 고정; §2.2 union이 한쪽만 구현되어
   CLEAR로 새는 경로 케이스화. `PainWorkspace.tsx:290~304` 마운트 조건 잔존 = 재발
   지점.
2. **통합 리셋 키 cross-patient 누수** — render-time reset 경로만 사용(key-remount
   금지), submission↔visit·fixtures 전환·환자 전환 3종 실측. N-6의 key 제거 누락
   시 조용한 리셋 실패.
3. **자동 펼침 조건식 누락** — 신규 disclosure 5개 각 1:1 테스트 + C-2 절단 규칙을
   같은 범주로 감시 (절단선 아래로 안전 정보가 내려가지 않는지).
